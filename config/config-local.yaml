# Local Development - API Mobile New
# Overrides config.yaml for local development

server:
  port: 8080

database:
  postgres:
    host: "localhost"
    password: "edugo123"
    ssl_mode: "disable"

  mongodb:
    uri: "mongodb://edugo:edugo123@localhost:27017/edugo?authSource=admin"

messaging:
  rabbitmq:
    url: "amqp://edugo:edugo123@localhost:5672/"

logging:
  level: "debug"
  format: "text"

storage:
  s3:
    access_key_id: "AKIAIOSFODNN7EXAMPLE"
    secret_access_key: "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"

auth:
  jwt:
    secret: "dev-secret-key-change-in-production"
  api_admin:
    base_url: "http://localhost:8081"
    timeout: 5s
    cache_ttl: 60s
    cache_enabled: true
